#include <bits/stdc++.h>

int main() {
    int t, a, b, c, d;
    scanf("%d", &t);
    while (t--)
    {
        int num;
        scanf("%d", &num);
        if (num * 4 <= 1000)
        {
            printf("Yes\n");
        } else {
            printf("No\n");
        }
        
    }
    
    return 0;
}