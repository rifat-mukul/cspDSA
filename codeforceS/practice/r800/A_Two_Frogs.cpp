#include<bits/stdc++.h>

int main() {
    int t;
    scanf("%d",&t);
    while (t--)
    {
        /* code */
        int n,a,b;
        scanf("%d%d%d",&n,&a,&b);
        if (abs(a-b)%2==0)
        {
            /* code */
            printf("YES\n");
        } else {
            printf("NO\n");
        }
        
    }
    
    return 0;
}