#include <bits/stdc++.h>

int main() {
    int t;
    scanf("%d",&t);
    while (t--)
    {
        /* code */
        int k,x;
        scanf("%d%d",&k,&x);
        // int result=0;
        for (int i = 0; i < k; i++)
        {
            /* code */
            x=(x*2);
        }

        printf("%d\n",x);
        
    }
    
    return 0;
}
