#include<bits/stdc++.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if (a>=1 && b>=1 && a<=3 && b<=3)
    {
        /* code */
        printf("Yes\n");
    } else if (a>=4 && b>=4 && a<=6 && b<=6)
    {
        /* code */
        printf("Yes\n");
    } else if (a>=7 && b>=7 && a<=9 && b<=9)
    {
        /* code */
        printf("Yes\n");
    } else {
        printf("No\n");
    }
    
    
    
    return 0;
}